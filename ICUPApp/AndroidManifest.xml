<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="ICUP.App" android:versionCode="1" android:versionName="1.0">

	<!-- The application requires the following permissions -->
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.VIBRATE" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_CHECKIN_PROPERTIES" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<!-- Defining all the activities in ICUP -->
	<application android:icon="@drawable/icon" android:label="@string/app_name">
		<!-- Uses the Google Android Map library -->
		<uses-library android:name="com.google.android.maps" />
		
		<!-- This is the default starting activity of ICUP -->
		<activity android:name=".ICUP" android:label="@string/app_name" android:launchMode="singleTop">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
				<category android:name="android.intent.category.DEFAULT" />
			</intent-filter>
		</activity>
		
		<!-- These are the other activities of ICUP -->
		<activity android:name=".Settings" android:label="@string/settings" android:launchMode="singleTop" android:configChanges="orientation"></activity>
		<activity android:name=".FeaturedEvent" android:label="@string/featuredcontent" android:launchMode="singleTop" android:configChanges="orientation|screenLayout|keyboardHidden"></activity>
		<activity android:name=".BusinessCat" android:label="@string/organizationcat" android:launchMode="singleTop"></activity>
		<activity android:name=".BusinessTabHost" android:label="@string/organizationdir" android:launchMode="singleTop" android:configChanges="orientation|screenLayout|keyboardHidden"></activity>
		<activity android:name=".EventTabHost" android:label="@string/eventlist" android:launchMode="singleTop" android:configChanges="orientation|screenLayout|keyboardHidden"></activity>
		<activity android:name=".About" android:label="@string/about" android:launchMode="singleTop" android:configChanges="orientation|screenLayout|keyboardHidden"></activity>
		<activity android:name=".Mapper" android:theme="@android:style/Theme.NoTitleBar" android:launchMode="singleTop" android:configChanges="orientation|screenLayout|keyboardHidden"></activity>
		<activity android:name=".EventsCat" android:label="@string/eventcat" android:launchMode="singleTop"></activity>
		<activity android:name=".FavoritesTabHost" android:label="@string/favorites" android:launchMode="singleTop" android:configChanges="orientation|screenLayout|keyboardHidden"></activity>
		<receiver android:name=".ICUPNotification" />
		
		<!-- Defining the minimum SDK version -->
		<uses-sdk android:minSdkVersion="7" />
	</application>
</manifest> 